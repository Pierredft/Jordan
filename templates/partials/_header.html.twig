<nav class="navbar navbar-expand-lg bg-jordan">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home')}}"><img class="logo" src="../images/logo/logo.svg" alt=""></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"    aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link color-jordan" aria-current="page" href="{{ path ('app_home')}}">Acceuil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link color-jordan" href="{{ path ('app_product_index')}}">Shop</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <div class="me-3">
                        <a class="nav-link color-jordan position-relative" href="{{ path ('app_cart') }}">
                            <i id="cart-icon" class="fa-solid fa-cart-shopping"></i>
                            {% if app.user and cart_quantity > 0 %}
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                    {{ cart_quantity }}
                                    <span class="visually-hidden">articles dans le panier</span>
                                </span>
                            {% endif %}
                        </a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle color-jordan" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fa-solid fa-user"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end color-jordan">
                        {% if app.user %}
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_user', {'id': app.user.id}) }}">Mon compte</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a class="dropdown-item" href="{{ path('admin') }}">Admin</a></li>
                            {% endif %}
                        {% else %}
                            <li><a class="dropdown-item" href="{{ path('app_register') }}">Inscription</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a></li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
