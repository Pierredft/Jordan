{% extends 'base.html.twig' %}

{% block title %}Jordan - Connexion{% endblock %}

{% block body %}
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">Aucun compte n'a était trouver.
            <a class="ms-2" href="{{path ('app_register')}}">Inscriver vous</a>
            </div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3">
                Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
        {% endif %}

        <h1 class="h3 mb-3 font-weight-normal text-center mt-3">Connecte toi 👋</h1>
        <div class="container">
        <label for="username" class="mb-2">Email</label>
        <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control border-jordan color-jordan-form" autocomplete="email" required autofocus>
        <label for="password" class="mb-2 mt-2">Mot de passe</label>
        <input type="password" name="_password" id="password" class="form-control mb-3 border-jordan color-jordan-form" autocomplete="current-password" required>
        <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">


            {# Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html #}

            <div class="checkbox mb-3">
                <input type="checkbox" name="_remember_me" id="_remember_me">
                <label for="_remember_me">Se souvenir de moi</label>
            </div>
        </div>

        <div class="d-flex justify-content-center">
            <button class="btn btn-lg btn-jordan" type="submit">Me connecter</button>
        </div>
    </form>
{% endblock %}
